# Upstream issue #15: a declaration carrying an annotation or `static` was
# skipped by the checks entirely. Every construct here is written twice, plain
# and then decorated, and the two halves must report the same thing.
#
# The message assertions are the second half of that bug, found later. The
# checks recognised the declaration and then read its contents off the raw line,
# where the first `(` belongs to the annotation:
#   :27  reported 1 parameter, measuring @rpc("any_peer")'s argument list
#   :43  reported a parameter named `""`, the remains of "any_peer"
#   :10  was skipped, because the `:` in "res://levels/x.tscn" reads as a type
#
# flags: --check too-many-params,unused-parameter,missing-type-hint,naming-signal,naming-const,naming-enum
# target: res://src
# exit: 1
src/probe.gd:4 naming-signal
src/probe.gd:5 naming-signal
src/probe.gd:7 missing-type-hint
src/probe.gd:8 missing-type-hint
src/probe.gd:9 missing-type-hint
src/probe.gd:10 missing-type-hint ~ 'pathy_untyped' has no type annotation
src/probe.gd:11 missing-type-hint
src/probe.gd:17 naming-const
src/probe.gd:18 naming-const
src/probe.gd:20 naming-enum
src/probe.gd:23 too-many-params ~ 6 parameters
src/probe.gd:27 too-many-params ~ 'annotated_many' has 6 parameters
src/probe.gd:31 too-many-params ~ 6 parameters
src/probe.gd:35 too-many-params ~ 'both_many' has 6 parameters
src/probe.gd:39 unused-parameter ~ 'never_a'
src/probe.gd:43 unused-parameter ~ 'never_b'
