# Extends an inner class of another file, which Godot cannot resolve either.
# Stays its own finding: folding it into inner_only.gd would assert a
# relationship the engine does not have.
extends Ghost
