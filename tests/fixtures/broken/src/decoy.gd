# This comment says extends Node, and a text scan used to believe it, which
# left this script reported as its own root cause instead of folded.
extends CascadeBase

const NOTE := "extends RefCounted"
